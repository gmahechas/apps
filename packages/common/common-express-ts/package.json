{
  "name": "@gmahechas/common-express-ts",
  "version": "0.0.3",
  "description": "common-express-ts",
  "author": "gmahechas",
  "license": "ISC",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "directories": {
    "lib": "dist"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build:development": "rm -rf dist && tsc --project tsconfig.development.json",
    "build:stage": "rm -rf dist && tsc --project tsconfig.stage.json",
    "build:production": "rm -rf dist && tsc --project tsconfig.production.json"
  },
  "dependencies": {
    "express": "4.18.2"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "@types/node": "20.10.4",
    "typescript": "5.3.3"
  },
  "engines": {
    "node": "20.x"
  }
}
